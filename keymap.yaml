states:
  NAV:
    hint: "Vm mode: h/j/k/l · d/y/c ops · v/V visual"
    keys:
      # Navigation (Delegated to Grammar)
      h: { action: "" }
      j: { action: "" }
      k: { action: "" }
      l: { action: "" }
      "0": { action: "" }
      "$": { action: "" }
      "^": { action: "" }
      w: { action: "" }
      b: { action: "" }
      e: { action: "" }
      G: { action: "" }
      
      # Operators (Delegated to Grammar)
      d: { action: "" }
      y: { action: "" }
      c: { action: "" }
      
      # Visual Mode: 未实现（readline/shell 场景无选区语义，
      # 假绑定会让 v/V 被静默吞掉；M4.1 诚实移除后 v/V 字面透传给 pane）
      
      # Prefix Keys
      g: { action: "" } # Handles gg, ge, etc via grammar
      f: { action: "" }
      F: { action: "" }
      t: { action: "" }
      T: { action: "" }
      
      # Misc
      u: { action: "" } # Delegated to Grammar
      "C-r": { action: "" } # Delegated to Grammar
      Escape: { action: "exit" }
      q: { action: "exit" }
      ":": { action: "prompt" }

  GOTO: # Legacy GOTO layer might be redundant if 'g' is handled by Grammar, but keeping for safety if 'g' timeout logic exists
    hint: "h far-left · l far-right · g top · G bottom"
    keys:
      h: { action: "far_left" }
      l: { action: "far_right" }
      g: { action: "goto_top" }
      G: { action: "goto_bottom" }
      q: { action: "exit" }
      Escape: { action: "exit" }
